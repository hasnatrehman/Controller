using ProjectCore.StateMachine;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;
[CreateAssetMenu(fileName = "GarageSelectionTransition", menuName = "ProjectCore/State Machine/Transitions/GarageSelection Transition")]
public class GarageSelectionTransition : Transition
{
    // Start is called before the first frame update
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {

    }
}
